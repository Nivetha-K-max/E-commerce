# ---- Server ----
server.port=8080
server.base-url=http://localhost:8080

# ---- MySQL ----
spring.datasource.url=jdbc:mysql://localhost:3306/dressshop
spring.datasource.username=root
spring.datasource.password=Nivi-1627

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ---- File uploads ----
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
upload.dir=./uploads

# ---- Admin login (change before deploying!) ----
admin.username=admin
admin.password=admin123
admin.token=change-this-to-a-long-random-string

# ---- JWT (customer auth) ----
jwt.secret=change-this-to-a-long-random-secret-at-least-32-chars

# ---- CORS ----
cors.allowed-origin=http://localhost:5172
